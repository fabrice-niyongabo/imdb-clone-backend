##
#showing sql commands
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
##
# DB connection
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
#server port
server.port=${PORT}